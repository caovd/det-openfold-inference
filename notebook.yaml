description: dcao-of-inf
environment:
  image: determinedai/environments:cuda-11.3-pytorch-1.10-deepspeed-0.8.3-gpu-0.24.0
  environment_variables:
    - NCCL_DEBUG=INFO
    - HF_HOME=/hf_cache
    # You may need to modify this to match your network configuration.
    #- NCCL_SOCKET_IFNAME=ens,eth,ib
resources:
  slots: 1
  resource_pool: A100